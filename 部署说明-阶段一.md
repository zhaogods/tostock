# 阶段一部署说明

## Git 提交

**分支**: feature/tushare-fina-indicator  
**Commit**: 734868e  
**内容**: 控制台数据健康度面板（阶段一）

### 新增文件
- instock/lib/data_asset_manager.py
- instock/lib/data_asset_registry.py
- instock/web/data_asset_handler.py

### 修改文件
- instock/web/web_service.py
- instock/web/templates/console.html

---

## Docker 镜像

**镜像名**: zsswwz/tostock:latest  
**构建命令**:
```bash
docker build -t zsswwz/tostock:latest -f docker/Dockerfile .
```

**推送命令**:
```bash
docker push zsswwz/tostock:latest
```

---

## 部署步骤

### 1. 拉取最新代码
```bash
git pull origin feature/tushare-fina-indicator
```

### 2. 使用 Docker Compose 部署
```bash
docker-compose up -d --build
```

### 3. 验证部署
访问: http://localhost:9988/instock/console

查看"📊 数据健康度"面板是否正常显示。

---

## 验证清单

- [ ] 控制台页面正常加载
- [ ] 数据健康度面板显示
- [ ] 显示 9 个数据资产
- [ ] API 接口正常响应
- [ ] 自动刷新功能正常

---

## 回滚方案

如有问题，回滚到上一个 commit:
```bash
git checkout c56608c
docker-compose up -d --build
```
